import React from "react";
import "./Header.css";

const Header = props => (
  <header className="header">
    <h1>Clicky Game</h1>
    <h2>
      Click the images to earn points, but don't click an image more then once!
    </h2>
  </header>
);

export default Header;
